<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.theskillsshop">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SkillsPersonRatingActivity"></activity>
        <activity android:name=".SkillsPersonRegistrationActivity" />
        <activity android:name=".CustomersBookingActivity" />
        <activity android:name=".CustomersRatingActivity" />
        <activity android:name=".MessagesActivity" />
        <activity android:name=".WorkinghoursActivity" />
        <activity android:name=".AboutusActivity" />
        <activity android:name=".ServicesActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".ContactsActivity" />
        <activity android:name=".OperationsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>